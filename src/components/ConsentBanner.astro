---
/**
 * Loads the shared consent banner. Markup, styles and behaviour all live in
 * public/shared/consent.js, which the standalone Salary Compass loads too — this
 * used to be a second copy of all three, kept in sync by a comment.
 *
 * Not a module: it has to run on the Salary Compass as well, which is plain HTML
 * with no bundler, so both consumers load the identical file.
 */
---

<script is:inline src="/shared/consent.js" defer></script>
